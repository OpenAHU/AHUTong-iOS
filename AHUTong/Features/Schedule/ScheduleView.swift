import SwiftUI

struct ScheduleView: View {
    var body: some View {
        FeatureLandingView(
            title: "课表",
            systemImage: "calendar",
            description: "课表模型、周次解析和离线缓存将在核心闭环阶段接入。"
        )
    }
}
