import SwiftUI

struct SettingsView: View {
    var body: some View {
        FeatureLandingView(
            title: "设置",
            systemImage: "gearshape",
            description: "账号、主题、提醒和隐私设置将在基础设施稳定后接入。"
        )
    }
}
